#include <bits/stdc++.h>
using namespace std;
int main(void) {
  ios::sync_with_stdio(0);
  cin.tie(0);
  char al;
  cin >> al;
  if (al == 'N' or al == 'n')
    cout << "Naver D2";
  else
    cout << "Naver Whale";
  return 0;
}